
'''Необходимо создать произвольный массив целочисленных чисел из 20 элементов от 0 до 100(используйте ф-ию random).Упорядочить данный массив по возрастанию
   и  найти определенный элемент (указать его индекс) или сообщить, что такого числа нет.'''

from random import random
N = 20
array = []
for i in range(N):
    array.append(int(random()*100))
array.sort()
print(array)

number = int(input('Введите число, которое необходимо найти: '))

low = 0
high = N-1
while low <= high:
    mid = (low + high) // 2
    if number < array[mid]:
        high = mid - 1
    elif number > array[mid]:
        low = mid + 1
    else:
        print("id =", mid)
        break
else:
    print("Такого числа нет")

'''Дан список целых чисел: [10, -10, 3, 8, 0, 9, -7, 16, -2, 5]
 Напишите функцию, которая возвращает массив, 
 где отрицательные цисла необходимо заменить на -1, положительные числа на 1, а ноль оставить без изменений.'''

listOrigin = [10, -10, 3, 8, 0, 9, -7, 16, -2, 5]

def returnNumber(arr):
    listMask = []
    for item in arr:
        if item > 0:
            listMask.append(1)
        elif item < 0:
            listMask.append(-1)
        else:
            listMask.append(0)
    return listMask

print(listOrigin)
print('Результат:',returnNumber(listOrigin))
'''
Напишите программу, которая просит вести сначала:
   - длину массива;
   - затем заполнить данный массив поэлементно положительными и отрицательными числами.
   - затем на основе полученного массива создать новый массив только из положительных чисел. 
   - найти из положительных чисел наибольший элемент и вывести на экран новый массив и наибольший элемент
'''
X = []
a = int(input('Введите длину предполагаемого массива: '))
for i in range(a):
    b = int(input('Введите элемент массива: '))
    X.append(b)
print('Ваш массив: ',X)
X_2 = []
m = 0
for j in range(a):
    if X[j] > m:
        m = X[j]
    if X[j] > 0:
        X_2.append(X[j])
print('Новый массив, состоящий из положительных элементов:', X_2)
print ('Наибольший элемент массива:', m)

